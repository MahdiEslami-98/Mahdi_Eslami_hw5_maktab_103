"use strict";
// DOM element

// const notifWrapper = document.getElementById("notif");
// const notifText = document.getElementById("notif-text");
// const btnElem = document.getElementById("hideBtn");
// const bodyElem = document.querySelector("body");

// user inputs

// const topPos = +prompt("Please enter position from top :");
// const rightPos = +prompt("Please enter position from right :");
// const htmlText = prompt("Please enter your notif text:");
// const cssClass = prompt(
//   "Please enter your class style for notif \n hint : (red, green, blue, purple):"
// );

//show notif in DOM function

// const showNotification = (top = 0, right = 0, html, className) => {
//   notifWrapper.style.display = "block";
//   notifWrapper.style.top = `${top}px`;
//   notifWrapper.style.right = `${right}px`;
//   notifText.innerText = `${html}`;
//   notifWrapper.classList.add(`${className || "grey"}`);
// };

// hide notif from DOM

// const hideNotifHandler = () => {
//   notifWrapper.style.display = "none";
// };

// const hideNotifWithKeyHandler = (e) => {
//   if (e.keyCode === 13 || e.keyCode === 27) {
//     notifWrapper.style.display = "none";
//   }
// };

// call functions

// btnElem.addEventListener("click", hideNotifHandler);
// bodyElem.addEventListener("keydown", hideNotifWithKeyHandler);
// showNotification(topPos, rightPos, htmlText, cssClass);
